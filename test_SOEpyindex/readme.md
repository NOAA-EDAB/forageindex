Forage Fish VAST index workflow

Date: 2025-09-04
Author: AST

This is a draft to summarize the wrangling I've done so far.

# Data processing

Data are processed with the script `process_input_data2.R` This script relies on `pisccomplete.csv` and `utils.R`. It still contains QA code comparisons to Sarah's original script, which can be removed when the script is used to create the 2026 indicators (copy the script to a new file and save this one in the archive).

# VAST model

# VAST output wrangling
